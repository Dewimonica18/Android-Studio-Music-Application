<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mortaries.a1910102017">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.1910102017">
        <activity android:name=".RayuanPulauKelapaActivity"
            android:parentActivityName=".MyMusicActivity"></activity>
        <activity android:name=".MajuTakGentarActivity"
            android:parentActivityName=".MyMusicActivity"/>
        <activity android:name=".IndonesiaRayaActivity"
            android:parentActivityName=".MyMusicActivity"/>
        <activity android:name=".IndonesiaPusakaActivity"
            android:parentActivityName=".MyMusicActivity"/>
        <activity
            android:name=".MusicPlayerActivity"
            android:parentActivityName=".MyMusicActivity"/>
        <activity
            android:name=".MyMusicActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>